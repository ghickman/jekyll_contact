---
layout: default
body_id: home
front_page: true
---
- site.posts.reverse.slice(0..4).each do |post|
  %article
    %header
      %h1
        %a{:href=>"#{post.url}", :title=>"#{post.title}"}=post.title
      .info
        %p
          Posted on:
          %time{:datetime=>"#{post.date.strftime('%Y-%m-%dT%H:%M:%S')}"}#{post.date.strftime('%b %d %Y')}
        %ul.post_tags
          %li.first Tagged as:
          - post.categories.each do |tag|
            %li
              %a{:href=>"/category/#{tag}", :title=>"#{tag}"}=category_capitalize(tag)
    .body= post.content